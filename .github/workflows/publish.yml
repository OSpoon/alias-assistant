name: 'publish'

on:
  workflow_dispatch:
  push:
    branches:
      - release

jobs:
  publish-tauri:
    permissions:
      contents: write

    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: setup node
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'
          cache-dependency-path: pnpm-lock.yaml

      - name: install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: install frontend dependencies
        run: pnpm install

      - name: Build and publish
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}
        with:
          tagName: alias-assistant-v__VERSION__
          releaseName: 'Alias Assistant v__VERSION__'
          releaseBody: |
            ## Alias Assistant v__VERSION__
            
            ### 📦 下载和安装
            
            1. 下载 `alias-assistant_*_aarch64.app.tar.gz` 文件
            2. 解压文件：`tar -xzf alias-assistant_*_aarch64.app.tar.gz`
            3. 将 `Alias Assistant.app` 拖拽到 `/Applications` 文件夹
            4. 首次打开时，如果提示"已损坏"，请右键点击应用 → 选择"打开"
            
            ### 📝 系统要求
            
            - macOS (Apple Silicon)
            - macOS 10.13 或更高版本
            
            ### 🔗 相关链接
            
            - [项目主页](https://github.com/ospoon/alias-assistant)
            - [使用说明](https://github.com/ospoon/alias-assistant#使用说明)
          releaseDraft: false
          prerelease: false
          args: '--target aarch64-apple-darwin'

